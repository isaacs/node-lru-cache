{
  "name": "lru-fs",
  "description": "A cache object that deletes the least-recently-used items. Supports fs swapping and default values.",
  "version": "6.0.0",
  "author": "Isaac Z. Schlueter <i@izs.me>",
  "keywords": [
    "mru",
    "lru",
    "cache",
    "fs"
  ],
  "scripts": {
    "test": "tap",
    "snap": "tap",
    "preversion": "npm test",
    "postversion": "npm publish",
    "prepublishOnly": "git push origin --follow-tags"
  },
  "main": "index.js",
  "repository": "git://github.com/schipiga/node-lru-cache.git",
  "devDependencies": {
    "benchmark": "^2.1.4",
    "tap": "^14.10.7"
  },
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^9.0.1",
    "lodash": "^4.17.19",
    "tmp": "^0.2.1",
    "yallist": "^4.0.0"
  },
  "files": [
    "index.js"
  ],
  "engines": {
    "node": ">=10"
  }
}
